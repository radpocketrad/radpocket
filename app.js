const buttons=document.querySelectorAll('.projection');const ap=document.getElementById('ap-content');const placeholder=document.getElementById('placeholder-content');const title=document.getElementById('placeholder-title');const back=document.getElementById('back-ap');buttons.forEach(btn=>btn.addEventListener('click',()=>{buttons.forEach(b=>b.classList.remove('active'));btn.classList.add('active');if(btn.dataset.projection==='ap'){ap.classList.remove('hidden');placeholder.classList.add('hidden')}else{ap.classList.add('hidden');placeholder.classList.remove('hidden');title.textContent=btn.dataset.projection==='mortise'?'Mortise view':'Lateral view'}}));back.addEventListener('click',()=>document.querySelector('[data-projection="ap"]').click());
